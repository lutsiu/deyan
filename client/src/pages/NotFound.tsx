import PageTitle from "../components/Common/PageTitle";

export default function NotFound() {
  return (
    <section className="flex-1 flex items-center justify-center min-h-[60vh] blogAndServicesPagesContainer section-padding">
      <PageTitle title="notFound.title" />
    </section>
  );
}
